{% extends "base.twig" %}
{% set body_class = 'push-down photo-essays-page' %}
{% set footer = 'false' %}

{% block content %}

	<section class="grey no-top-space no-bottom-space">
		<div class="section-header">
			<div class="container">
				<div class="flex-grid baseline">
					<div class="col-3 align-left">
						<a class="prev-link" href="#">Previous Cat</a>
					</div>
					<div class="col-6">
						<h1 class="h3 large-title">Photo Essays</h1>
					</div>
					<div class="col-3 align-right">
						<a class="next-link" href="#">Next Cat</a>
					</div>
				</div>
			</div>
		</div>
		<div class="container no-bottom-space">
			<div class="inline-grid project-grid">
				{% for item in 1..12 %}
					<div class="col-4 right">
						{% include 'components/article-list-thumb.twig' with {
								'size' : '1200-800.jpg',
								'category' : 'Category',
								'title' : 'Article Title',
								'author' : 'Dayyan Armstrong'
							} only
						%}
					</div>
				{% endfor %}
			</div>
			<div class="more-cta after-list">
			<div class="container">
				{% include 'components/button.twig' 
					with {
						'buttonClass': 'large transition-link',
						'buttonText': 'More Photo Essays',
						'buttonLink': globals.path ~ '/#'
					} only
				%}
			</div>
		</div>
		</div>
	</section>

	{% include 'partials/super-footer.twig' %}


{% endblock %}{# end content #}